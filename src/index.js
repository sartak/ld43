import React from 'react';
import ReactDOM from 'react-dom';
import './style.css';
// import Stream from './Stream';
import Deployed from './Deployed';

const Layout = Deployed;
//    ? Stream

ReactDOM.render(<Layout />, document.getElementById('root'));
